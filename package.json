{
  "type": "module",
  "workspaces": [
    "site"
  ],
  "scripts": {
    "coverage-ci": "vitest run --coverage",
    "fuzz": "node scripts/fuzz/run.mjs 30",
    "fuzz:ci": "node scripts/fuzz/run.mjs 30",
    "lint": "biome check . && markdownlint-cli2",
    "lint:fix": "biome check --write . && markdownlint-cli2 --fix",
    "sync": "tsx scripts/skills-sync.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@fast-check/vitest": "^0.4.1",
    "@jazzer.js/core": "^4.0.0",
    "@types/node": "^26.1.0",
    "@vitest/coverage-v8": "^4.1.9",
    "esbuild": "^0.28.1",
    "fast-check": "^4.8.0",
    "markdownlint-cli2": "^0.23.0",
    "tsx": "^4.22.5",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
